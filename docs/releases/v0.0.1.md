relq 0.0.1 is out.

This is the initial release of relq: a type-safe SQL query builder for SQLite
and PostgreSQL on Python 3.15 and later. It includes the core query builder,
SQLite and PostgreSQL executors, and schema code generation.

Please give it a try and report anything that looks off.
